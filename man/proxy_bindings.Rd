\name{proxy_bindings}
\alias{proxy_bindings}
\title{Generate binding for proxies.}
\usage{
  proxy_bindings(mf, j = names(mf))
}
\arguments{
  \item{mf}{mutaframe to inherit from}

  \item{j}{columns to generate bindings for}
}
\description{
  Generate binding for proxies.
}

